+section('services', titles.service)
  .col
    +row
      - for (var i = 0; i < 4; i++)
        if i
          +service('Назва послуги ' + (i + 1))
        else
          +service('Сімейні спори')
  +serviceForm()
